// New generated images are self-contained data URLs; no bundled fallback image
// is substituted for a model result.
export function shareImageReference(imageUrl: string) {
  return imageUrl;
}

export function resolveShareImage(imageUrl: string) {
  return imageUrl;
}
